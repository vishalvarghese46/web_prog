export const get_full_name = (first_name, last_name) => first_name + " " + last_name;

export const get_last_name = (last_name) => `, ${last_name}`;

export const get_first_name = (first_name) => `${first_name},`;
